services:
  - type: web
    name: telegram-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python milkshake.py
    envVars:
      - key: PORT
        value: 10000
    # Add this to force Python version
    pythonVersion: "3.11.9"
